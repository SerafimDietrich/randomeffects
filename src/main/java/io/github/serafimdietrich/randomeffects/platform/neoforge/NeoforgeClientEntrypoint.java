package io.github.serafimdietrich.randomeffects.platform.neoforge;

        //? neoforge {

/*import io.github.serafimdietrich.randomeffects.RandomEffects;
import net.neoforged.api.distmarker.Dist;
import net.neoforged.fml.common.Mod;
import net.neoforged.neoforge.common.NeoForge;

@Mod(value = RandomEffects.MOD_ID, dist = Dist.CLIENT)
public class NeoforgeClientEntrypoint {
    public NeoforgeClientEntrypoint() {
        RandomEffects.onInitializeClient();
        NeoForge.EVENT_BUS.register(NeoforgeClientEventSubscriber.class);
    }
}
        *///?}
